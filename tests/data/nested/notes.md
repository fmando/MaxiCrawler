# Nested notes

This file exists to prove that directory traversal is recursive.

It links to [the plugin guide](https://docs.example.test/plugins), which the
top-level HTML index already references, so the discovery run must report it as
a duplicate rather than a second unique URL.

It also contributes one URL that appears nowhere else:
https://notes.example.test/nested
